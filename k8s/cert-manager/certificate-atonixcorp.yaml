apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: atonixcorp-com-cert
  namespace: default
spec:
  secretName: atonixcorp-com-tls
  dnsNames:
  - api.atonixcorp.org
  - www.atonixcorp.org
  - atonixcorp.org
  issuerRef:
    name: letsencrypt-dns-cloudflare-staging
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048
  commonName: atonixcorp.org
